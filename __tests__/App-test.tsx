/**
 * @format
 */

import 'react-native';
import React from 'react';
import App from '../App';
import renderer from 'react-test-renderer';

describe('MockTest', () => {
  it('renders correctly', () => {
    expect(2).toBe(2);
    // renderer.create(<App />);
  });
})


